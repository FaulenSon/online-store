@model Shop.Models.ProductViewModel
        <link rel="stylesheet" href="~/css/nicepage.css" media="screen">
        <link rel="stylesheet" href="~/css/Главная.css" media="screen">
        <script class="u-script" type="text/javascript" src="~/js/jquery.js" defer=""></script>
        <script class="u-script" type="text/javascript" src="~/js/nicepage.js" defer=""></script>
        <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
        <div class="row">
            <div class="col-md-3">
                <div class="list-group my-4">
                    <a asp-action="GetOrders" class="list-group-item list-group-item-action">Заказы</a>
                    <a asp-action="GetUsers" class="list-group-item list-group-item-action">Пользователи</a>
                    <a asp-action="GetRoles" class="list-group-item list-group-item-action">Роли</a>
                    <a asp-action="AddProduct" class="list-group-item list-group-item-action">Добавление товаров</a>
                    <a asp-action="DeleteProduct" class="list-group-item list-group-item-action">Удаление товаров</a>
                </div>
            </div>
            <div class="col-md-9">
                <div class="card my-4">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-12">
                                <h4>Удаление товара</h4>
                                <hr />
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-12 table-responsive">
                                <table class="table table-hover table-sm">
                                    <thead>
                                        <tr>
                                            <th>№</th>
                                            <th>Наименование</th>
                                            <th>Цена</th>
                                        </tr>
                                    </thead>
                                    <tbody asp-controller="Product">
                                        @foreach (var prod in Model.ProductList)
                                        {
                            <tr class="table-row" asp-controller="Product" asp-route-id="@prod.Id">
                                <td asp-controller="Product" asp-route-id="@prod.Id">@prod.Id</td>
                                <td asp-controller="Product" asp-route-id="@prod.Id">@prod.Name</td>
                                <td asp-controller="Product" asp-route-id="@prod.Id">@prod.Price BYN</td>
                            </tr>
}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
